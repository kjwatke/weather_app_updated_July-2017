/* @flow*/
import React from 'react';

const WeatherInfo = () =>
  (<div
    style={{
      border: '2px solid red',
    }}
  >
    <h1>Weather Info component in red....</h1>
    <p />
  </div>);

export default WeatherInfo;
